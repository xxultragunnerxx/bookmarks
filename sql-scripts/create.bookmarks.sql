CREATE TABLE IF NOT EXISTS bookmarks (
  bookmark_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT UNIQUE NOT NULL,
  url TEXT NOT NULL,
  description TEXT,
  rating TEXT NOT NULL
);
